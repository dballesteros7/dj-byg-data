"""
A bunch of commonly used helper functions
"""

def tag_to_genre(tag):
    """
    Given one of the last.fm tags defined in $PROJECT_HOME/data/lastfm-tags.txt,
       returns one of the genres listed in $PROJECT_HOME/data/lastfm-genres.txt.
       Returns None if tag is unrecognized.
    """
    return tag
